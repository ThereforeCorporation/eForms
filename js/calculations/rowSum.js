//To calculate a sum or any calculation inside a grid, you can use the row.
//quantity = API Name of column with quantity
//unitPrice = API Name of column with unit price
value = row.quantity * row.unitPrice;
